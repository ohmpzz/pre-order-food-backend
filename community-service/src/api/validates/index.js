import addUser from './add-user.validate';
import community from './community.validate';
import member from './member.validate';
import token from './token.validate';

const validate = {
  addUser,
  community,
  member,
  token,
};

export default validate;
